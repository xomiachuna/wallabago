# yaml-language-server: $schema=https://raw.githubusercontent.com/evilmartians/lefthook/master/schema.json
colors: true
no_tty: true
# hook that runs before every commit
pre-commit:
  jobs:
    - name: check-quick
      run: make check-quick
      stage_fixed: true

# hook that runs after the commit message is provided
commit-msg: {}
